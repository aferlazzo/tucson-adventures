{
  "name": "tucson-adventures",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "node --check dist/app.js",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
